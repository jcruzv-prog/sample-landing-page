import ListItemButton from '@mui/material/ListItemButton';

export default function ListItem({children}){

    return(
        <ListItemButton >
            {children}
        </ListItemButton>
    )
}